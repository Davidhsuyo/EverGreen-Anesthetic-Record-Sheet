# Active exec plans

One file per in-flight, non-trivial task: goal, approach, current state, open
questions. Move the file to `../completed/` (and add outcome/commit info) once merged.
Skip this for small, self-contained fixes — it's for work that spans more than one
session or needs a paper trail while in progress.
